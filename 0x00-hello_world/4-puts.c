#include <stdio.h>

/**
 * main -this is the main function
 *
 * Return: return to 0 when successful
 */

int main(void)
{
	char MyStrl [] = "\"Programming is like building a multilingual puzzle"
	
	puts(MyStril);
	
	return (0);
}
