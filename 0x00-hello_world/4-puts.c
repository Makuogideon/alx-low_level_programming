#include <stdio.h>

/**
 * main -this is the main function
 *
 * Return: return to 0 when successful
 */

int main(void)
{
	puts("\"programming is like building a multilingual puzzle");
	return (0);
}
